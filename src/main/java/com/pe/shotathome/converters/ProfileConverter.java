package com.pe.shotathome.converters;
/*
import com.pe.shotathome.dto.ProfileDto;
import com.pe.shotathome.entity.Profile;

public class ProfileConverter {
    public static Profile DtoToEntity(ProfileDto prf) {
        Profile profile=new Profile();
        profile.setFirts_name(prf.getFirsts_name());
        profile.setLast_name(prf.getLast_name());
        profile.setPhone(prf.getPhone());
        profile.setAddress(prf.getAddress());
        profile.setRole(prf.getRole());
        profile.setCountry(prf.getCountry());
        profile.setCity(prf.getCity());
        return profile;
    }
    public static ProfileDto EntityToDto(Profile prf) {
        ProfileDto profileDto=new ProfileDto();
        profileDto.setFirts_name(prf.getFirsts_name());
        profileDto.setLast_name(prf.getLast_name());
        profileDto.setPhone(prf.getPhone());
        profileDto.setAddress(prf.getAddress());
        profileDto.setRole(prf.getRole());
        profileDto.setCountry(prf.getCountry());
        profileDto.setCity(prf.getCity());
        return profileDto;
    }
}*/
